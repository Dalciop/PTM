# Uncomment the next line to define a global platform for your project
platform :macos, '12.3'

target 'PTM' do
  # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!
	pod 'SwiftSoup'
  # Pods for PTM

  target 'PTMTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PTMUITests' do
    # Pods for testing
  end

end
